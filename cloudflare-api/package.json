{
  "name": "visionhub-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview",
    "db:migrate": "wrangler d1 execute visionhub-db --file=./migrations/schema.sql",
    "db:seed": "wrangler d1 execute visionhub-db --file=./migrations/seed.sql",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240109.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
